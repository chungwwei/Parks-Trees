CREATE TABLE street_trees (
    tree_id INTEGER,
    block_id INTEGER,
    created_at VARCHAR,
    tree_dbh INTEGER,
    stump_diam INTEGER,
    curb_loc VARCHAR,
    status VARCHAR,
    health VARCHAR,
    spc_latin VARCHAR,
    spc_common VARCHAR,
    steward VARCHAR,
    guards VARCHAR,
    sidewalk VARCHAR,
    user_type VARCHAR,
    problems VARCHAR,
    root_stone VARCHAR,
    root_grate VARCHAR,
    root_other VARCHAR,
    trunk_wire VARCHAR,
    trnk_light VARCHAR,
    trnk_other VARCHAR,
    brch_light VARCHAR,
    brch_shoe VARCHAR,
    brch_other VARCHAR,
    address VARCHAR,
    postcode VARCHAR,
    zip_city VARCHAR,
    community_board INTEGER,
    borocode INTEGER,
    borough VARCHAR,
    cncldist INTEGER,
    st_assem INTEGER,
    st_senate INTEGER,
    nta VARCHAR,
    nta_name VARCHAR,
    boro_ct VARCHAR,
    state VARCHAR,
    latitude FLOAT,
    longitude FLOAT,
    x_sp VARCHAR,
    y_sp VARCHAR,
    council_district INTEGER,
    census_tract VARCHAR,
    bin INTEGER,
    bbl VARCHAR
);
